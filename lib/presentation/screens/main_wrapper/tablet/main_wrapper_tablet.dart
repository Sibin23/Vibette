import 'package:flutter/material.dart';

class MainWrapperTablet extends StatelessWidget {
  const MainWrapperTablet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MainWrapperTablet'),
      ),
    );
  }
}
